<!DOCTYPE html>
<html>
<head>
    <title>Global Rewards - Your Points Loyalty Program</title>
    <link href="./style.css" rel="stylesheet">
    <script src="https://apis.google.com/js/client:platform.js" async defer></script>
    <script>
      function signinCallback(authResult) {
        if (authResult['status']['signed_in']) {
          // Update the app to reflect a signed in user
          // Hide the sign-in button now that the user is authorized, for example:
          window.location.replace("/logged-in?token=" + authResult['access_token']);
        } else {
          // Update the app to reflect a signed out user
          // Possible error values:
          //   "user_signed_out" - User is signed-out
          //   "access_denied" - User denied access to your app
          //   "immediate_failed" - Could not automatically log in the user
          console.log('Sign-in state: ' + authResult['error']);
        }
      }
  </script>
</head>
 <body>

    <div class="container">
      <div class="header">
        <ul class="nav nav-pills pull-right">
          <% if !@session %>
            <li><a href="/login">Log In</a></li>   
          <% else %>
            <li><a href="/profile">Your Profile</a></li> 
            <li><a href="/give">Give Points</a></li> 
          <% end %>
          <li><a href="http://www.points.com/">Points Home</a></li>
        </ul>
        <h3 class="text-muted"><a href="/"><img src="globalrewards_logo.png" alt="GlobalRewards" /></a></h3>
      </div>

      <div class="jumbotron">
        <%= yield %>
      </div>

      <div class="row marketing">
        <div class="col-lg-6">
          <h4>Be loyal to our partners!</h4>
          <p>By joining Global Rewards, you'll live as our members do and get a better sense of the obstacles and opportunities our pratners face.</p>

        </div>

        <div class="col-lg-6">
          <h4>Share the love, and we will, too!</h4>
          <p>Get engaged at Points and you'll find yourself handsomely rewarded with great perks, neat events, and even some potential rewards!</p>

        </div>
      </div>

      <div class="footer" align="center">
        <p>&copy; Global Rewards, 2015</p>
      </div>

    </div> <!-- /container -->
</body>
</html>


